4.2 Não equivalente. É necessário que haja a subtração de i dentro do array para o funcionamento correto
do código.

Código mutado: int temp[] = new int [m+i+1];

6.1 Não equivalente. Trocando <= por < fará com que o for deixe de fazer a última iteração.

Código mutado: for (k = i; k < m; k++) temp[k-i] = v[k];

6.4 Não equivalente. Negando a condicional, o for não fará todas iterações.

Código mutado: for (k = i; k > m; k++) temp[k-i] = v[k];

6.5 Não equivalente. Colocando false na condicional, o for não fará as iterações.

Código mutado: for (k = i; false; k++) temp[k-i] = v[k];

10.1 Não equivalente. Trocar <= por < fará com que algumas entradas levem o código para o else, ao invés
do if, como aconteceria no código original.

Código mutado: if (temp[esq] < v[dir]) v[i++] = temp[esq++];
