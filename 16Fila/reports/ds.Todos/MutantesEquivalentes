13.1 Equivalente. Por default, o Java inicializa variáveis de objeto com null, assim como
ocorria no programa original.

Código mutado: Removida a linha 13.

19.1 Equivalente. Por default, o Java inicializa variáveis de objeto como null, assim como
ocorria no programa original.

Código mutado: Removida a linha 19.

35.2 Equivalente. Com false no while, nada será impresso, mas o return será o mesmo.

Código mutado: while (false) {

36.3 Equivalente. A mensagem impressa estará diferente da do código original, mas o return do método será o mesmo.

Código mutado: System.out.print(" " + aux.item);

36.5 Equivalente. A mensagem impressa estará diferente da do código original, mas o return do método será o mesmo.

Código mutado: System.out.print(" " + aux.item);

36.6 Equivalente. A remoção desta linha fará com que nada seja impresso por este método, mas o return será igual.

Código mutado: removida a linha 36.

39.1 Equivalente. Assim como em 36.6, remover a linha fará com que nada seja impresso, mas manterá o return do código.

Código mutado: removida a linha 39.
